import Menu from './Menu';
import Timer from './TimerExample'
function About({}) {
      return (
         <div>
             <Menu/>
            <h2>About</h2>
            <Timer/>
         </div>
      );
   }
export default About;